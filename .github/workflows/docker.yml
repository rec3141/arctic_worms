name: docker

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  build-test-push:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Build image (load into local docker)
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: arctic_worms:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke-test the RAxML/pplacer pipeline
        run: |
          docker run --rm arctic_worms:ci bash -c '
            set -e
            cd /arctic_worms
            scripts/run_pipeline.sh /tmp/macro \
              data/sequences/macrostomorpha_references.fas \
              data/sequences/catenulida_outgroups.fasta \
              data/sequences/amplicons_platyhelminthes.fasta \
              /dev/null 2
            test -s /tmp/macro/tog.nwk && test -s /tmp/macro/placements.csv
            echo "SMOKE TEST PASSED: $(grep -o ">" /tmp/macro/deduped.fasta | wc -l) refs, tree + placements produced"'

      - name: Smoke-test figure generation (R + sf/PROJ + showtext)
        run: |
          docker run --rm arctic_worms:ci bash -c '
            set -e
            cd /arctic_worms
            Rscript scripts/plot_placement_tree.R macrostomorpha
            Rscript scripts/plot_kbd_maps.R
            test -s figures/macrostomorpha_placement_tree.pdf && test -s figures/acoela_map_pie.pdf
            echo "FIGURE TEST PASSED: $(ls figures/*.pdf | wc -l) PDFs"'

      - name: Log in to GHCR
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image to GHCR
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:latest
          cache-from: type=gha
